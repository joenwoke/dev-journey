// Objects: Key value data containers

// Object example
let person = {
    name: "Merlin",
    age: 22,
    isStudent: true
};

// To access values
console.log(person.name);

// Alternatively
console.log(person["age"]);

// Modify values
person.age = 23;
console.log(person.age);
